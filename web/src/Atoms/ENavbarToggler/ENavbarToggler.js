import { NavbarToggler } from 'reactstrap'
import styled from 'styled-components'

const ENavbarToggler = ({ ...rest }) => {
    return <StyleNavbarToggler {...rest} />
}

export default ENavbarToggler

const StyleNavbarToggler = styled(NavbarToggler)``
